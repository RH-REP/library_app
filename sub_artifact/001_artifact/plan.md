# Plan

## 作業手順

1. 初回 issue の回答を読み、作りたいもの、進め方、ゴールを抽出する。
2. `main_artifact/.goal_template.md` を参照して `main_artifact/goal.md` を作成する。
3. `main_artifact/.development_process_template.md` を参照して `main_artifact/development_process.md` を作成する。
4. `sub_artifact/001_artifact/` の標準ファイルを初期化する。
5. `issue_log/` に人間向けの作業記録を残す。

## 判断方針

- ユーザーが明示していない要件は確定しない。
- 初回は細かい実装仕様ではなく、次に確認しやすい原案を優先する。
- AI 秘書は補助として扱い、AI に頼らない検索導線を必須条件として残す。
- 3 段階レビューの希望を工程に反映する。

## 完了条件

- `main_artifact/goal.md` と `main_artifact/development_process.md` が存在する。
- 仮置き内容と未確定事項が分かれている。
- `sub_artifact/001_artifact/` が標準ファイル構成で初期化されている。
- Issue #1 への返答に使える要約がある。
